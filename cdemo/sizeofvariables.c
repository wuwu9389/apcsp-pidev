#include<stdio.h>

int main()
{
  unsigned a;
  unsigned j;
  char k = 'b';

  // print value and size of an int variable
  printf("int a value: %d and size: %d bytes, my float is %d, my char is %d\n", a, sizeof(a), j, k);

}
